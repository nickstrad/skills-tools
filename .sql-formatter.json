{
  "expressionWidth": 80,
  "paramTypes": {
    "positional": true,
    "numbered": ["?", "$"],
    "named": [":", "@", "$"],
    "custom": [
      { "regex": ":'[A-Za-z_][A-Za-z0-9_]*'" },
      { "regex": ":\"[A-Za-z_][A-Za-z0-9_]*\"" }
    ]
  }
}
