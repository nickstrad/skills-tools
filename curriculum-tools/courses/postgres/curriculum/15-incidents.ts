import { code, type Module } from "../../../src/types.ts";

// See ../PLAN.md section "incidents". Lessons are filled in by the module author.
export const INCIDENTS: Module = {
  category: "reliability",
  title: "Capstone incidents",
  lessons: [],
};
void code;
