import { code, type Module } from "../../../src/types.ts";

// See ../PLAN.md section "wal". Lessons are filled in by the module author.
export const WAL: Module = {
  category: "wal",
  title: "The write-ahead log: records, durability, crash redo",
  lessons: [],
};
void code;
