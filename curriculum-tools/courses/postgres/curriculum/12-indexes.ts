import { code, type Module } from "../../../src/types.ts";

// See ../PLAN.md section "indexes". Lessons are filled in by the module author.
export const INDEXES: Module = {
  category: "indexes",
  title: "B-tree internals, HOT, and concurrent builds",
  lessons: [],
};
void code;
