# Lessons 27–31: verify recovery and replica progress

Agreed route continuation requested 2026-09-13: exactly rows 27–31 of PLAN.md. Primary authors and
reviews the batch; no delegation requested. Preserve every existing lesson and learner history.
Follow [the batch workflow](../../../../docs/lesson-batch-workflow.md).

Each lesson targets 12–15 minutes including explanation, a 3–4 minute native-command task,
debugging, observations and cleanup. A supplied Go controller owns PostgreSQL 16 process control,
fixtures and teardown. The learner edits one consequential command/argument; a safe incomplete
starter and clearly labelled worked completion are in the same lesson. No answer submission or
progress gate. Validate both the starter's limitation and the worked command's real effect.

| Lesson | Mechanism and learner task | Decisive evidence | Boundary and cleanup |
| --- | --- | --- | --- |
| 27 restore-and-verify | Physical backup restored separately; construct an ordered inventory query that distinguishes equal counts with changed payloads | Baseline equals restored inventory; later source differs despite equal count | Small three-row fixture, streamed WAL backup, separate socket; stop/remove all copies |
| 28 recovery-needs-history | Recovery copy lacks a required archived segment; construct the copy command that restores exactly the withheld file | Target recovery fails with missing history, then reaches the target after repair; original archive hash unchanged | Remove only a file from a stopped disposable archive copy, never source pg_wal; stop/remove full tree |
| 29 targeted-recovery | Choose a named restore point before a bad committed operation | Full source history versus recovered IDs, recovery target log and paused recovery state | Two named candidate points after backup, target pause, no promotion/failover; stop/remove full tree |
| 30 build-a-standby | Construct pg_basebackup options that also configure streaming recovery | standby.signal, recovery mode, streaming receiver and post-backup marker visible | Same-host two-server fixture; no availability/fencing claim, no persistent slot; stop/remove both |
| 31 received-is-not-replayed | Diagnose paused apply and issue the SQL action that resumes it | Receive LSN reaches post-commit marker while replay and rows lag; after resume replay/rows catch up | Tiny paused workload with deadline; resume during cleanup, then stop/remove both |

Owned files: new lessons 27–31; lab/recovery Go package; validation/batch-seven*;
this design, temporary handoff, and availability updates in PLAN/README/docs. No shared engine
changes planned. Existing dirty tutor.sqlite, docs/knowledge/postgres-lab.md and analysis.md are
unrelated and preserved.

Preflight: about 9.9 GiB disk and 7 GiB memory available; inodes 8% used. Learner server verified
read-only at /labs/pglab/primary, socket /tmp port 5440. Baseline tutor.sqlite SHA256:
65809427c05348e0484e6a7b57d81607eab52ba360264d4e7330f961ee3ffd99.
Run serial fixtures with a 500 MB peak budget (primary, base backup, recovery copy, two small
archives); require at least 3 GiB free before allocation, leaving 2 GiB headroom. Keep only small
logs and source hashes after acceptance. Verify learner readiness/history at completion.

Technical basis: PostgreSQL 16 [continuous archiving](https://www.postgresql.org/docs/16/continuous-archiving.html),
[pg_basebackup](https://www.postgresql.org/docs/16/app-pgbasebackup.html),
[standby operation](https://www.postgresql.org/docs/16/warm-standby.html), and
[recovery controls](https://www.postgresql.org/docs/16/functions-admin.html).
These inform authoring; lessons are self-contained and reading remains optional.
