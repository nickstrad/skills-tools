import type { Module } from "../../../src/types.ts";

export const NAMESPACES: Module = {
  category: "namespaces-and-isolation",
  title: "Change which kernel resources a process can see",
  lessons: [],
};
