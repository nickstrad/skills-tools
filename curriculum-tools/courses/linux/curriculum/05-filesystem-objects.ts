import type { Module } from "../../../src/types.ts";

export const FILESYSTEM: Module = {
  category: "filesystem-objects",
  title: "Separate names, inodes, permissions, and open files",
  lessons: [],
};
