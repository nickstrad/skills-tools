import type { Module } from "../../../src/types.ts";

export const MOUNTS: Module = {
  category: "mounts-and-storage-paths",
  title: "Trace storage accounting through mounts and filesystems",
  lessons: [],
};
