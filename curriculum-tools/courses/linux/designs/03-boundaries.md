# Change 03: budgets, sockets and isolation

Own only `curriculum/09-resource-boundaries.ts` through `11-namespaces.ts` and
`validation/03-boundaries.md`. Read OVERPLAN, PLAN, AUTHORING and shell gotchas.

Every lesson needs the exact three authoring headings with full command/flag/output explanations and
defined concepts. Add specific **Predict**, **Inspect and explain**, **Vary**, **Hint**, **Apply**
challenges. Supply a runnable bounded variation and execute it; use the existing service/resource
setting to ask for evidence-backed decisions. New namespace mechanisms still receive complete
commands. Preserve slugs/order and course default revision; increment explicit lesson revisions.

Audit resource-domain claims: rlimits, per-real-UID process accounting, shared cgroup accounting,
namespace visibility and authority are different boundaries. A socket listener does not prove useful
service. Backlog outcomes and permissions vary; do not promise exact queue sizes or turn policy
skips into proof. Keep networking to loopback/endpoints and namespace isolation; no routing or
container expansion. Preserve sound code and report proposed nontrivial semantic changes to the
primary.

Use a private copy and LINUX_LAB; build, check owned source, execute all owned lessons and
variations, and report commands and per-lesson output. Privileged resource experiments must not
overlap the resource-evidence agent's privileged run: coordinate before running. Copy only owned
modules/report back, never shared artifacts or commits. Primary reviews every lesson and integration
evidence.
